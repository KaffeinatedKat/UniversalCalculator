function WordProblem(prob) { /* WordProblem function, <<< OperationFind() >>> */
    var newproblem = Problem.value
    var numbrs = newproblem.match(/\d+/g)
    if (prob.includes("more") || ("sum") || ("adds") || ("put")) {

    } else if (prob.includes("take") || ("give") || ("remove")) {

    }
}
